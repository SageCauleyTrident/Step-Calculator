# Step-Calculator
Takes two given measurements and the counts per step to calculate the new counts per step to properly calibrate all the axis

# Create a new standalone
py -m PyInstaller --onefile --windowed your_app.pyw
